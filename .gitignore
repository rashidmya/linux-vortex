# Cloned upstream checkout (large; never committed)
/upstream/
# Build outputs
/out/
/dist/
*.AppImage
# Downloaded runtimes / tools
/vendor/
/tools/bin/
# Internal planning docs (brainstorming spec + implementation plan) — kept local, not distributed
/docs/superpowers/
# OS / editor noise
.DS_Store
